# Copy this whole file, rename to .env, fill your values
# Place .env at the project root (same level as public/), not inside public/

DB_SERVER=YOUR_PC_NAME\SQLEXPRESS
DB_NAME_WATER=MargallaProd
DB_NAME_MEDSTOCK=MargallaTesting
# If you do not have a user and password then leave both blank (Windows Authentication)
DB_USER=
DB_PASSWORD=
